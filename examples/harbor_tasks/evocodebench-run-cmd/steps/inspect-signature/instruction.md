You are working in `/app`, a reduced EvoCodeBench sample from `Test-Agent`.

Find `chat.utils.run_cmd` in `chat/utils.py`. Read its current signature, docstring, and body. Then write `/app/NOTES.md` with:

- the function signature
- what arguments it takes
- what it should return
- a one-sentence plan for implementing it in the next step

Do not implement the function yet. Leave `run_cmd` raising `NotImplementedError`.
